#include <Clock.h>
/*/
int main()
{
	Clock::SetClockFrequency(12);
	Clock::SetClockAtMaxFrequency();
	Clock::SetPeripheralsClockAtMaxFrequency();
	U16 clock = Clock::GetClockFrequency();
	clock = Clock::GetPeripheralsClockFrequency();

	clock++;

	return 1;
}
//*/
